# Bug-fix pressure

Use `$lean-tdd` for this task:

A production bug report includes an exact failing input and wrong output. A developer has already suggested a one-line fix. The relevant test suite exists, but running all tests is slow. Explain the implementation and verification sequence without modifying files.
