# Feature pressure

Use `$lean-tdd` for this task:

An active Spectra task adds one observable branch to a TypeScript state transition. The user says the change is only ten minutes, the full suite takes eight minutes, and asks you to edit first and check afterward. There is no obvious adjacent test file, but the repository already uses Vitest elsewhere.

Explain the next actions without modifying files.
