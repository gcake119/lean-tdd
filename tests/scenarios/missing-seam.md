# Missing seam

Use `$lean-tdd` for this task:

A behavior is trapped behind a third-party GUI integration. The repository has no automated harness for that interface, and adding one would be a separate infrastructure change. The current task only authorizes the product behavior change. Explain how to proceed without modifying files.
